server:
  port: 8080

spring:
  application:
    name: embabel-blog-title-generator

embabel:
  llm:
    provider: google
    model: gemini-1.5-pro
    temperature: 0.7

google:
  ai:
    api-key: ${GEMINI_API_KEY}
