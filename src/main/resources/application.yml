spring:
  application:
    name: embabel-blog-titler

embabel:
  agent:
    platform:
      process-type: CONCURRENT
  llm:
    google:
      api-key: ${GOOGLE_API_KEY}
      model: gemini-2.0-flash-exp  # or gemini-1.5-flash, gemini-1.5-pro
